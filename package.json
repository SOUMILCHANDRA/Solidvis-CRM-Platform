{
  "name": "solidvis-crm-platform",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && rm -rf dist && cp -r frontend/dist dist"
  }
}
